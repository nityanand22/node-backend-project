## HTTP CRASH COURSE

// HTTP ke andar data simple text format me aata hai but HTTPs ke andar data encrypt format me aata hai jisse security badh jati hai.

// Dono me same protocol use hota hai.

// HTTP ke andar data chota hai to HTTPs ke andar data bada hota hai.

// HTTP Headers =>
// metadata = key-value pair , sent along with the request & response.
// Caching, Authenticating, Manage State => HTTP headers caching ke liye use hota hai. Headers authentication ke liye use hota hai. Headers manage state ke liye use hota hai. like cookies and session management.
// Request Headers => form client
// Response Headers => form server
// Representation of data => encoding | compression ( JSON, XML, HTML, CSS, JS) => Data representation ke liye use hota hai.
// Payload Headers => data

Most Common Headers

- Accept: application/json
- Users: Agent (browser information)
- Authorization : Bearer token
- Content-type
- Cookie
- Cache control

CORS

- Access-Control-Allow-Origin
- Access-Control-Allow-Methods
- Access-Control-Allow-Headers
- Access-Control-Max-Age
- Access-Control-Allow-Credentials
- Access-Control-Expose-Headers

Security

- Cross-Origin-Embedder-Policy
- Cross-Origin-Opener-Policy
- Cross-Origin-Resource-Policy
- X-XSS-Protection
- Content-Security-Policy
- X-Content-Security-Policy


